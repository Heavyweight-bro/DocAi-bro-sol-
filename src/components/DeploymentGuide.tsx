import React from 'react';
export default function DeploymentGuide() {
 return <div className="deployment-page"><div className="page-heading"><div><h2>Впровадження в компанії</h2><p>Що вже працює, що потрібно додати та як взаємодіють частини системи.</p></div></div>
 <div className="setup-banner">Поточна версія: один робочий простір, вибір AI-провайдера та послідовна обробка в браузері. Облікові записи, розмежування доступу й серверна черга ще не реалізовані.</div>
 <section className="corporate-panel"><h3>Окремий застосунок для однієї компанії</h3><p>Та сама програма, але власна адреса (наприклад, docs.company.ua), база даних, сховище файлів та ключі AI. У налаштуваннях уже можна змінити назву компанії. Це змінює вивіску, а не створює ізоляцію даних.</p><p>Для повноцінного запуску адміністратор розгортає сервер, підключає БД і HTTPS, а розробник додає вхід співробітників та права доступу. Ключі провайдерів залишаються на сервері.</p></section>
 <section className="corporate-panel"><h3>Коли користувачів багато</h3><div className="comparison-wrap"><table><thead><tr><th>Одна компанія</th><th>Сервіс для кількох компаній</th></tr></thead><tbody><tr><td>Запрошення співробітників і ролі: адміністратор, оператор, перегляд.</td><td>Додатково: окремий простір організації та членство користувача.</td></tr><tr><td>Права на документи й шаблони перевіряє сервер.</td><td>Усі записи, файли, завдання й API-ключі прив’язані до organization_id.</td></tr><tr><td>Журнал дій, резервні копії, ліміти обробки.</td><td>Ізоляція через політики БД, квоти та перевірки доступу для кожної компанії.</td></tr></tbody></table></div><p>Самого поля «компанія» недостатньо: користувач не має отримувати чужі дані навіть прямим API-запитом.</p></section>
 <section className="corporate-panel"><h3>База даних — реєстр роботи</h3><p>Зберігає користувачів, шаблони, статуси та витягнуті дані. Самі PDF і скани краще тримати в окремому файловому сховищі, а в БД — посилання на них.</p><p>Практичний варіант: керований PostgreSQL, наприклад Supabase. Адміністратор створює проєкт і регіон, застосовує схему таблиць та права, вмикає резервні копії й передає серверу реквізити підключення. Локальний JSON-файл у поточній версії — лише для роботи на одному комп’ютері.</p></section>
 <section className="corporate-panel"><h3>Черга — диспетчер завдань</h3><p>Зараз файли йдуть один за одним, поки відкрита сторінка. Серверна черга дозволить закрити браузер: сервер збереже завдання, а окремий процес-виконавець (worker) обробить його незалежно від користувача.</p><ol><li>Файл зберігається у сховищі, завдання отримує ID і статус «Очікує».</li><li>Worker бере завдання, читає шаблон і надсилає документ обраному AI.</li><li>Результат записується в БД, статус змінюється на «Готово» або «Помилка».</li><li>Тимчасові помилки повторюються з паузою; невірний ключ потребує виправлення.</li></ol><p>Для реалізації: Redis + BullMQ і worker, або черга на PostgreSQL. Також потрібні захист від повторної обробки, обмеження паралельних запитів і моніторинг. Підключення Redis саме по собі не додає чергу — її логіку треба реалізувати.</p></section>
 <section className="corporate-panel"><h3>Як працює API шаблону</h3><p>API — це «вхідні двері» для іншої програми. Ви створюєте шаблон «Рахунок» з ідентифікатором invoice. Адреса <code>POST /api/parse/invoice</code> приймає файл і застосовує саме цей шаблон. Нова копія сервера не створюється.</p><p>CRM надсилає файл → сервер знаходить шаблон → AI витягує поля → сервер зберігає результат → CRM отримує JSON з ID документа. Сто рахунків використовують одну адресу, але мають сто різних ID. Збережений результат доступний через GET /api/documents/:id без повторного виклику AI.</p><p>Зараз відповідь приходить після завершення обробки. Після додавання серверної черги планується відповідь <code>202 + jobId</code>; результат можна буде отримати запитом статусу або webhook. Ці асинхронні адреси поки не реалізовані.</p></section>
 </div>;
}
